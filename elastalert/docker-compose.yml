version: "3.7"

services:
  elastalert:
    image: vimagick/elastalert
    volumes:
      - ./data:/opt/elastalert
    restart: unless-stopped

networks:
  default:
    external:
      name: elk_default

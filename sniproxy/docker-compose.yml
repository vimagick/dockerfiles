services:
  sniproxy:
    image: easypi/sniproxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./data/etc:/etc/sniproxy
      - ./data/log:/var/log/sniproxy
    restart: unless-stopped

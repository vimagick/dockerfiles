revive:
  image: vimagick/revive
  ports:
    - "8080:80"
  links:
    - mysql
  volumes:
    - /var/www/html/var
  restart: always

mysql:
  image: mysql
  environment:
    - MYSQL_ROOT_PASSWORD=root
    - MYSQL_DATABASE=revive
  restart: always

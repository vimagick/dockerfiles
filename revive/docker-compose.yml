revive:
  image: vimagick/revive
  ports:
    - "8080:80"
  links:
    - mysql
  restart: always

mysql:
  image: mysql
  environment:
    - MYSQL_ROOT_PASSWORD=root
    - MYSQL_DATABASE=revive
  restart: always

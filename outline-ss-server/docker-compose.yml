services:
  outline-ss-server:
    image: easypi/outline-ss-server
    ports:
      - "8388:8388/tcp"
      - "8388:8388/udp"
      - "9092:9092/tcp"
    volumes:
      - ./data:/app/etc
    restart: unless-stopped

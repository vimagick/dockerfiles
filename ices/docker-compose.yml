services:

  ices:
    image: easypi/ices
    volumes:
      - ./data:/etc/ices
    devices:
      - /dev/snd
    depends_on:
      - icecast
    restart: unless-stopped
  
  icecast:
    image: easypi/icecast
    ports:
      - "8000:8000"
    restart: unless-stopped

hass:
  image: easypi/hass-arm:debian
  volumes:
    - ./data:/etc/hass:ro
  net: host
  privileged: yes
  restart: unless-stopped

hcfg:
  image: easypi/hcfg-arm
  volumes:
    - ./data:/etc/hass:rw
  net: host
  working_dir: /etc/hass
  restart: unless-stopped

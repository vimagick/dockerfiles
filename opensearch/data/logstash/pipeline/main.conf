input {
  beats {
    port => 5044
    host => "0.0.0.0"
  }
}

filter {

}

output {
  opensearch {
    hosts => ["http://opensearch-node1:9200", "http://opensearch-node2:9200"]
    index => "logstash-%{+YYYY.MM.dd}"
  }

  stdout {
    codec => rubydebug
  }
}
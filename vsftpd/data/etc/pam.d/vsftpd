# ONLY VIRTUAL USERS ARE ALLOWED
#auth required pam_pwdfile.so pwdfile=/etc/vsftpd/passwd  crypt=crypt
#account required pam_permit.so

auth    sufficient  pam_pwdfile.so pwdfile=/etc/vsftpd/passwd crypt=crypt
auth    required    pam_unix.so
account required    pam_permit.so
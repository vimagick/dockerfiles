services:
  cmus:
    image: easypi/cmus
    ports:
      - "3000:3000"
    volumes:
      - ./data/etc:/root/.config/cmus
      - ./data/var:/root/music
    devices:
      - /dev/snd
    stdin_open: true
    tty: true
    restart: unless-stopped

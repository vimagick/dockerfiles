cmus:
  image: vimagick/cmus
  ports:
    - "3000:3000"
  volumes:
    - ./data:/root/.cmus
    - ~/music:/root/music
  devices:
    - /dev/snd
  environment:
    - PASSWORD=secret
  tty: yes
  restart: unless-stopped
